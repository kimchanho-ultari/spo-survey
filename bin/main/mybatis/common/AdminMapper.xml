<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ultari.additional.mapper.common.AdminMapper">
	<insert id="regist">
UPDATE MSG_ADMIN
SET
		PASSWORD=#{password}
WHERE
		USER_ID=#{key}
	</insert>
</mapper>