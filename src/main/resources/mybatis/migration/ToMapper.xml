<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ultari.additional.mapper.migration.to.ToMapper">
	<delete id="removeDept">
DELETE FROM MSG_PART
	</delete>
	<insert id="registDept">
INSERT INTO MSG_PART(PART_ID, PART_NAME, PART_HIGH, PART_ORDER) VALUES
<foreach collection="list" item="item" index="index" separator=",">
(
	  #{item.key}
	, #{item.title}
	, #{item.pKey}
	, #{item.sort}
)
</foreach>
	</insert>
	<delete id="removeMember">
DELETE FROM MSG_USER WHERE USER_TYPE='ORG'
	</delete>
	<insert id="registMember">
INSERT INTO MSG_USER(USER_ID, USER_NAME, USER_HIGH, PHONE, MOBILE, EMAIL, POS_NAME, JOB, PASSWORD, USER_ORDER, USER_TYPE) VALUES
(
	  #{key}
	, #{title}
	, #{deptId}
	, #{phone}
	, #{mobile}
	, #{email}
	, #{posName}
	, #{job}
	, #{password}
	, #{sort}
	, 'ORG'
) ON DUPLICATE KEY UPDATE
	  USER_NAME=#{title}
	, USER_HIGH=#{deptId}
	, PHONE=#{phone}
	, MOBILE=#{mobile}
	, EMAIL=#{email}
	, POS_NAME=#{posName}
	, JOB=#{job}
	, USER_ORDER=#{sort}
	, USER_TYPE='ORG'
	</insert>
</mapper>